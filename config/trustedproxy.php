<?php

/**
 * ⭐ پروکسی‌های مورد اعتماد (۲۰۲۶-۰۹-۲۶) — App\Http\Middleware\TrustProxies.
 *
 * پشت Cloudflare / nginx میزبان / هر پروکسی TLS، بدون این تنظیم Laravel آی‌پی و https واقعی مشتری رو نمی‌بینه:
 * - $request->ip() آی‌پی لبه‌ی Cloudflare می‌شد → محدودیت ورود (throttle:auth، ۵ بار در دقیقه، کلید = IP) بین همه‌ی
 *   مشتری‌هایی که از یک لبه می‌اومدن مشترک بود و چند ورود عادی بقیه رو قفل می‌کرد.
 * - scheme همیشه http → آدرس‌های بازگشت درگاه‌ها، لینک‌ها و کوکی‌های secure اشتباه.
 *
 * TRUSTED_PROXIES (با کاما): «cloudflare» (پیش‌فرض)، IP یا CIDR (مثلاً 172.16.0.0/12 برای nginx میزبان جلوی
 * Docker)، یا «*» فقط وقتی برنامه جز از طریق پروکسی در دسترس نیست. «none» یعنی به هیچ پروکسی اعتماد نکن.
 *
 * چرا پیش‌فرض cloudflare و نه «*»: فقط لبه‌ی Cloudflare از این رنج‌ها وصل می‌شه و Symfony آی‌پی مشتری رو از
 * سمت راست X-Forwarded-For (اولین آدرسِ غیرمعتمد) برمی‌داره؛ پس سرایندی که مهاجم خودش جعل کنه اثری نداره،
 * و روی سروری که پشت Cloudflare نیست رفتار دقیقاً مثل قبله. «*» یعنی هر کسی با یک سرآیند جعلی محدودیت ورود رو دور بزنه.
 */
return [

    'proxies' => env('TRUSTED_PROXIES') ?: 'cloudflare',

    /*
     * رنج‌های رسمی https://www.cloudflare.com/ips (بررسی‌شده ۲۰۲۶-۰۹-۲۶: ۱۵ رنج IPv4، ۷ رنج IPv6).
     * Cloudflare به‌ندرت تغییرش می‌ده؛ اگر تغییر داد همین فهرست به‌روز می‌شه.
     */
    'cloudflare' => [
        '173.245.48.0/20', '103.21.244.0/22', '103.22.200.0/22', '103.31.4.0/22', '141.101.64.0/18',
        '108.162.192.0/18', '190.93.240.0/20', '188.114.96.0/20', '197.234.240.0/22', '198.41.128.0/17',
        '162.158.0.0/15', '104.16.0.0/13', '104.24.0.0/14', '172.64.0.0/13', '131.0.72.0/22',
        '2400:cb00::/32', '2606:4700::/32', '2803:f800::/32', '2405:b500::/32', '2405:8100::/32',
        '2a06:98c0::/29', '2c0f:f248::/32',
    ],
];
