# پرامپت: تست دستی درگاه‌های زیبال، وندار و آسان‌پرداخت (ماهرو / Rasta)

> **روش استفاده:** این فایل را در یک چت تازه در پروژه‌ی «نوبت دهی انلاین سالن زیبایی» بفرست، همراه با zip پروژه.
> بعد از هر سناریو، نتیجه را با «قالب گزارش» پایین همین فایل برای Claude بنویس.

---

## بخش ۱ — دستور برای Claude

تو دستیار تست دستی درگاه‌های پرداخت پروژه‌ی Rasta هستی. طبق `Rasta_unified_prompt.md` کار کن.
درایورهای زیبال، وندار و آسان‌پرداخت با تست‌های خودکار بررسی شده‌اند، ولی آن تست‌ها سرور درگاه را شبیه‌سازی
می‌کنند (`Http::fake`) و هیچ‌کدام تا حالا به درگاه واقعی وصل نشده است. هدف این جلسه: اولین اجرای واقعی هر درگاه
با مبلغ کم، و پیدا کردن هر تفاوتی بین درگاه واقعی و فرض‌های کد.

قواعد این جلسه:

1. **قبل از هر تغییر کد، مشکل را بازتولید کن.** پاسخ واقعی درگاه (که ابوالفضل از لاگ یا دیتابیس می‌فرستد) را عیناً
   در یک تست با `Http::fake` بگذار و نشان بده تست قبل از اصلاح شکست می‌خورد.
2. بعد اصلاح کن، تست رگرسیون بنویس، سوییت کامل را روی کلون تازه اجرا کن، و طبق روال پروژه پچ (`git format-patch`) در
   یک پوشه‌ی جدا تحویل بده. پیام commitها انگلیسی.
3. اگر رفتار واقعی با مستند درگاه یکی نیست، مستند و پاسخ واقعی را کنار هم بگذار و قبل از تغییر، تصمیم را با ابوالفضل
   مطرح کن (او یک تصمیم دوحالته می‌دهد و بعد «ادامه بده» می‌گوید).
4. هیچ‌وقت کلید، توکن، PIN یا رمز درگاه را در پاسخ، سند یا commit ننویس. اگر ابوالفضل اشتباهاً فرستاد، به او بگو
   باطلش کند.
5. سناریوها را به ترتیب همین فایل جلو ببر. بعد از هر گزارش بگو: قبول، رد (با علت)، یا نیاز به اطلاعات بیشتر (دقیقاً چه چیزی).
6. در پایان جلسه جدول وضعیت همه‌ی سناریوها را بده و بخش «قدم‌های باز» سند پروژه را به‌روز کن.

---

## بخش ۲ — آماده‌سازی (یک بار، کار ابوالفضل)

- [ ] تست روی **سرور واقعی با دامنه‌ی HTTPS** انجام شود، نه XAMPP. درگاه‌ها دامنه و IP ثبت‌شده می‌خواهند.
- [ ] پچ‌های آخر (ملت، Docker، پارسیان/وندار/trustProxies) روی سرور اعمال شده باشند.
- [ ] `php artisan migrate --force` و `php artisan config:cache` اجرا شده باشد.
- [ ] **کرون و صف واقعاً کار کنند** (راهنمای `docs/deployment/SCHEDULER_AND_QUEUE.md`). بدون صف، پیامک‌ها ارسال نمی‌شوند
      و بدون کرون، reconcile و تمدید توکن وندار اجرا نمی‌شوند.
- [ ] اگر سایت پشت Cloudflare است، پیش‌فرض کافی است. اگر nginx خود سرور جلوی برنامه است، آدرس آن را به
      `TRUSTED_PROXIES` اضافه کن.
- [ ] در پنل هر درگاه: **IP سرور** و **دامنه‌ی سالن** ثبت شده باشد.
- [ ] یک سالن آزمایشی با یک خدمت ارزان (پیش‌پرداخت حدود ۱۰ هزار تومان) و یک متخصص.
- [ ] برای تست تسویه: «حداقل مبلغ برداشت» در تنظیمات کیف پول مدیریت را موقتاً ۴٬۰۰۰ تومان یا کمتر کن (بعد از تست
      برگردان). وگرنه برداشت‌های کوچک این جلسه قبل از رسیدن به درگاه رد می‌شوند.
- [ ] دو ترمینال روی سرور باز باشد:
  ```bash
  tail -f storage/logs/laravel.log
  ```
  ```bash
  # آخرین تراکنش پرداخت
  php artisan tinker --execute="dump(App\Models\PaymentTransaction::latest('id')->first()?->only(['id','driver','purpose','status','amount_rial','token','ref_id','gateway_receipt','card_pan','verify_response','updated_at']));"
  ```

---

## بخش ۳ — سناریوها

برای هر درگاه، در «درگاه‌های پرداخت» مدیریت سالن فقط همان درگاه را فعال و اولویت اول کن.

### الف) مشترک بین هر سه درگاه

| # | سناریو | کار | انتظار |
|---|---|---|---|
| ۱ | تست اتصال | دکمه‌ی «تست» کنار درگاه در `admin/payment-gateways` | پیام «اتصال … برقرار است (پولی جابه‌جا نشد)» |
| ۲ | پرداخت موفق نوبت | رزرو نوبت با پیش‌پرداخت، پرداخت واقعی | نوبت «پرداخت‌شده»، در tinker: `status = paid`، `ref_id` و `card_pan` پر |
| ۳ | انصراف | رفتن به درگاه و زدن «انصراف» | نوبت پرداخت‌نشده، تراکنش `cancelled`، پیام انصراف (نه خطای فنی) |
| ۴ | refresh صفحه‌ی نتیجه | بعد از سناریوی ۲، صفحه‌ی نتیجه را refresh کن و دکمه‌ی back مرورگر را بزن | هیچ تایید دوباره‌ای در لاگ نیست، نوبت یک بار ثبت شده، پیامک دوباره نمی‌رود |
| ۵ | شارژ کیف پول | شارژ کیف پول مشتری با مبلغ کم | موجودی به همان مبلغ زیاد شده، فقط یک بار |
| ۶ | failover | اطلاعات درگاه اول را عمداً غلط کن و یک درگاه درستِ دوم با اولویت بعدی بگذار | مشتری بی‌خطا به درگاه دوم می‌رود (فقط اگر درگاه اول «در دسترس نیست» تشخیص داده شود) |
| ۷ | مبلغ کارمزد | اگر برای درگاه کارمزد ثابت/درصدی گذاشته‌ای | مبلغ صفحه‌ی بانک = پیش‌پرداخت + کارمزد، با همان عدد tinker (`amount_rial` ÷ ۱۰ = تومان) |

### ب) زیبال

- [ ] اول همه‌ی سناریوهای الف را با **مرچنت تست رسمی `zibal`** اجرا کن (پول واقعی جابه‌جا نمی‌شود).
- [ ] بعد سناریوهای ۱ تا ۴ را با مرچنت واقعی تکرار کن.
- [ ] **تسویه‌ی واقعی (مهم‌ترین تست زیبال):** در «درگاه‌های پرداخت» توکن API و شناسه‌ی کیف پول تسویه را وارد کن. یک متخصص
      درخواست برداشت ۱۰٬۰۰۰ تومان بدهد، بعد در `admin/wallet/withdrawals` دکمه‌ی تسویه‌ی خودکار را بزن.
  - ⚠️ واحد مبلغ تسویه‌ی زیبال در مستندش صریح نیست؛ کد **ریال** می‌فرستد (۱۰۰٬۰۰۰).
  - در پنل زیبال ببین واریز **۱۰٬۰۰۰ تومان** ثبت شد یا ۱٬۰۰۰ یا ۱۰۰٬۰۰۰ تومان. این عدد را حتماً گزارش کن.
  - ```bash
    php artisan tinker --execute="dump(App\Models\WithdrawalRequest::latest('id')->first()?->only(['id','status','amount','reference_code','payment_details']));"
    ```

### ج) وندار

- [ ] سناریوهای الف با درگاه وندار (کلید درگاه). اگر وندار کلید آزمایشی نمی‌دهد، با مبلغ کم واقعی.
- [ ] **تسویه‌ی واقعی:** نام انگلیسی کسب‌وکار (داشبورد وندار ← تنظیمات ← اطلاعات پایه) و **هر دو** توکن
      (Access و Refresh) را وارد کن. برداشت ۱۰٬۰۰۰ تومانی، تسویه‌ی خودکار.
  - انتظار: در داشبورد وندار یک تسویه‌ی **۱۰٬۰۰۰ تومانی** با شناسه‌ی پیگیری `mahru-wd-{شماره‌ی برداشت}`.
    (کد تومان می‌فرستد و نمونه‌ی رسمی وندار هم همین را نشان می‌دهد؛ این اولین تأیید عملی است.)
  - برداشت در سایت `completed` با کد پیگیری.
  - تسویه‌ی وندار ۳۰ دقیقه قابل لغو است. اگر مبلغ غلط بود، فوراً از داشبورد وندار لغوش کن و گزارش بده.
- [ ] **تمدید توکن:**
  ```bash
  php artisan payouts:refresh-vandar-tokens
  ```
  انتظار: «۱ تمدید شد». بعد یک تسویه‌ی دیگر هم باید موفق باشد (یعنی توکن‌های تازه درست ذخیره شده‌اند).
  ⚠️ اگر «ناموفق» بود، توکن‌های قبلی ممکن است مصرف شده باشند؛ توکن تازه از داشبورد وندار بگیر و متن لاگ را گزارش کن.
- [ ] (اختیاری) حداقل مبلغ: برداشت ۴٬۰۰۰ تومانی باید بدون تماس با وندار با پیام «حداقل ۵٬۰۰۰ تومان» رد شود.

### د) آسان‌پرداخت

- [ ] آسان‌پرداخت محیط تست عمومی ندارد؛ با مبلغ کم واقعی.
- [ ] سناریوهای الف. نکته‌های مخصوص:
  - تست اتصال (۱) زمان سرور آسان‌پرداخت و اعتبار نام کاربری/رمز را می‌سنجد. پیام «IP سرور در پنل آسان پرداخت ثبت نشده
    است» (کد ۴۷۴) یعنی IP سرور را در پنل آسان‌پرداخت ثبت کن.
  - در سناریوی ۲ بعد از پرداخت، در پنل آسان‌پرداخت ببین تراکنش **تسویه‌شده/Settled** است، نه فقط تاییدشده (کد بعد از
    Verify درخواست Settlement هم می‌فرستد؛ در tinker مقدار `verify_response.settlement_status` را هم گزارش کن).

---

## بخش ۴ — قالب گزارش (هر سناریو یک گزارش)

```
درگاه: زیبال / وندار / آسان‌پرداخت
سناریو: شماره و نام
نتیجه: ✅ مطابق انتظار / ❌ متفاوت
آنچه دیدم: (پیام صفحه، وضعیت نوبت، مبلغ صفحه‌ی بانک)
خروجی tinker: (کامل؛ توکن‌ها را می‌توانی با *** بپوشانی)
خط‌های مرتبط لاگ: (از laravel.log، حدود ۲۰ خط اطراف خطا)
تصویر: (اختیاری — صفحه‌ی بانک یا پنل درگاه)
```

⚠️ کلید، توکن، PIN و رمز درگاه را در گزارش نفرست.

---

## بخش ۵ — چیزهایی که این جلسه تست **نمی‌کند** (برای شفافیت)

- حالت «پاسخ درگاه نرسید» (نتیجه‌ی نامعلوم تسویه و reconcile پرداخت) را نمی‌شود دستی و امن ساخت. تست خودکار دارد.
- درگاه‌های مستقیم بانکی (سامان، ملت، پارسیان) قرارداد جدا می‌خواهند و چک‌لیست خودشان بعداً نوشته می‌شود.
