# راهنمای Wildcard Subdomain — محور «۳. ساب‌دامین اختصاصی»

این فایل مرجع تنظیمات **خارج از کد** (DNS، وب‌سرور، SSL) برای محور ۳ است — چیزهایی که Claude
نمی‌تواند مستقیم انجامشان دهد چون به پنل دامنه و سرور واقعی ابوالفضل دسترسی ندارد. کد سمت
Laravel (middleware تشخیص سالن از ساب‌دامین) جدا مستند می‌شود؛ این فایل فقط زیرساخت است.

هدف نهایی: هر سالن به‌جای `yourdomain.com/s/rasta` روی `rasta.yourdomain.com` در دسترس باشد،
با fallback به مسیر فعلی `/s/{slug}` (طبق تصمیم مستند در `Rasta_unified_prompt.md`).

---

## وضعیت فعلی: فقط لوکال (XAMPP، ۲۰۲۶-۰۹-۱۹)

پروژه هنوز روی هیچ دامنه‌ی واقعی نیست، فقط روی `localhost` یا مشابه آن اجرا می‌شود. Wildcard DNS
واقعی برای لوکال بی‌معناست (فایل `hosts` ویندوز از wildcard پشتیبانی نمی‌کند)، پس برای تست محور ۳
روی لوکال از **nip.io** استفاده می‌کنیم — یک سرویس DNS عمومی و رایگان که هر زیردامنه‌ی
`<هرچیزی>.127.0.0.1.nip.io` را به‌صورت خودکار به `127.0.0.1` resolve می‌کند، بدون نیاز به تنظیم
هیچ‌چیزی روی سیستم.

مثال: بعد از پیاده‌سازی کد محور ۳، آدرس تست سالن «rasta» روی لوکال می‌شود:
```
http://rasta.127.0.0.1.nip.io/
```
(اگر XAMPP روی پورت غیر ۸۰ اجرا می‌شود، پورت را هم اضافه کن: `http://rasta.127.0.0.1.nip.io:8080/`)

**این فقط برای تست محلیه — هیچ تنظیم DNS یا وب‌سروری لازم نداره، و در Virtual Host فعلی XAMPP هم
تغییری لازم نیست چون nip.io هر ساب‌دامینی رو به همون IP لوکال برمی‌گردونه و Apache با
`ServerName localhost` هم درخواست‌هایی که Host header متفاوت دارن رو (اگه فقط یک VirtualHost روی
اون پورت باشه) قبول می‌کنه.** اگر چند VirtualHost روی XAMPP داری، باید `ServerAlias *.127.0.0.1.nip.io`
هم به همون VirtualHost این پروژه اضافه بشه (دقیقاً همون کاری که در بخش production زیر برای
Apache توضیح داده شده، فقط با دامنه‌ی nip.io به‌جای دامنه‌ی واقعی).

**سمت Laravel**: در `.env` (نه `.env.example`) این خط رو اضافه کن:
```env
CENTRAL_DOMAIN=127.0.0.1.nip.io
```
بعد یک سالن با یک slug واقعی بساز و از مرورگر به `http://<slug>.127.0.0.1.nip.io:8000` سر بزن.
دامنه‌ی مرکزی بدون ساب‌دامین: `http://127.0.0.1.nip.io:8000` (باید صفحه‌ی placeholder رو نشون
بده). مسیر قدیمی هم هم‌زمان کار می‌کنه: `http://127.0.0.1.nip.io:8000/s/<slug>/`.

---

## راهنمای Production (برای وقتی روی سرور واقعی/دامنه‌ی واقعی deploy شد)

### پیش‌نیاز: این ۳ مورد را قبل از شروع کدنویسی محور ۳ روی production باید داشته باشی
1. یک دامنه‌ی واقعی ثبت‌شده (مثلاً `rasta-app.ir`)
2. دسترسی به پنل DNS همان دامنه (جایی که رکوردهای A/CNAME را می‌سازی)
3. دسترسی SSH/کنترل‌پنل به سروری که Laravel رویش deploy می‌شود (VPS یا هاست)

### قدم ۱ — رکورد Wildcard DNS
توی پنل DNS دامنه (Cloudflare، ir-nic، cPanel، یا هرجای دیگه) یک رکورد جدید بساز:

| نوع | Name/Host | Value | TTL |
|---|---|---|---|
| `A` | `*` | آی‌پی سرور (مثلاً `93.xxx.xxx.xxx`) | Auto/3600 |

اگر از Cloudflare استفاده می‌کنی و می‌خوای از SSL/CDN رایگانشون هم بهره ببری، رکورد `*` را
**Proxied (ابر نارنجی)** بگذار — SSL wildcard را خودش مدیریت می‌کند (قدم ۳ را در این حالت لازم
نداری، Cloudflare گواهی خودش را می‌دهد).

اگر Cloudflare یا مشابهش نیست (DNS ساده)، یک رکورد `A` برای خود دامنه‌ی اصلی (`@`) هم لازم است تا
`rasta-app.ir` (بدون ساب‌دامین) هم کار کند — این معمولاً از قبل هست.

**نکته:** انتشار DNS (propagation) گاهی چند دقیقه تا چند ساعت طول می‌کشد. با
`nslookup test.rasta-app.ir` می‌توانی چک کنی رکورد جواب می‌دهد یا نه.

### قدم ۲ — وب‌سرور: قبول‌کردن هر ساب‌دامینی

**Apache** — به VirtualHost فعلی پروژه `ServerAlias` اضافه کن:
```apache
<VirtualHost *:80>
    ServerName rasta-app.ir
    ServerAlias *.rasta-app.ir
    DocumentRoot /var/www/Beauty-Salon/public
    ...
</VirtualHost>
```

**Nginx** — در `server_name` هم دامنه‌ی اصلی هم wildcard را بگذار:
```nginx
server {
    listen 80;
    server_name rasta-app.ir *.rasta-app.ir;
    root /var/www/Beauty-Salon/public;
    ...
}
```
برای بلاک `443 ssl` هم دقیقاً همین‌طور (بعد از گرفتن گواهی SSL در قدم ۳).

### قدم ۳ — گواهی SSL Wildcard
گواهی معمولی Let's Encrypt (`certbot --nginx` یا `--apache`) فقط برای دامنه‌های صریح کار می‌کند،
نه برای `*.rasta-app.ir`. برای گواهی wildcard واقعی باید از **DNS-01 challenge** استفاده کنی
(چون Let's Encrypt باید مالکیت کل دامنه را با یک رکورد TXT تأیید کند، نه فقط یک فایل روی سرور):

```bash
sudo certbot certonly --manual --preferred-challenges dns \
  -d "rasta-app.ir" -d "*.rasta-app.ir"
```
سپس certbot یک مقدار TXT می‌دهد که باید موقتاً در پنل DNS (رکورد `_acme-challenge`) اضافه کنی.

اگر DNS روی Cloudflare است، ساده‌تر: پلاگین `certbot-dns-cloudflare` را نصب کن تا این کار
خودکار با API Token انجام شود (نیازی به کار دستی TXT نیست) — یا همان‌طور که در قدم ۱ گفته شد،
ساده‌ترین راه استفاده از خودِ SSL رایگان Cloudflare (Proxied) است که این مرحله را کلاً حذف می‌کند.

گواهی wildcard باید هر ۹۰ روز تمدید شود؛ `certbot renew` را در `cron` بگذار (با DNS API پلاگین،
تمدید کاملاً خودکار می‌شود؛ با روش دستی TXT، هر بار نیاز به کار دستی دارد — برای همین روش
Cloudflare/DNS-API را توصیه می‌کنیم، نه manual).

### قدم ۴ — تنظیمات Laravel (`.env`)
```env
APP_URL=https://rasta-app.ir
CENTRAL_DOMAIN=rasta-app.ir
SESSION_DOMAIN=null
```
⚠️ **نکته‌ی امنیتی مهم (حل‌شده، ۲۰۲۶-۰۹-۱۹)**: تصمیم نهایی با ابوالفضل **ایزوله** بود — یعنی
`SESSION_DOMAIN` **نباید** روی `.rasta-app.ir` (با نقطه‌ی ابتدایی، مشترک بین ساب‌دامین‌ها)
ست بشه؛ مقدار پیش‌فرض پروژه (`null`) همین رفتار ایزوله رو می‌ده و نباید تغییر کنه. دلیل رد شدن
حالت مشترک: کوکی سشن مشترک بین ساب‌دامین‌ها ریسک نشتی/تداخل بین سالن‌های مختلف داره. عوضش یک
ریسک دیگه (کوچیک‌تر، پذیرفته‌شده) داریم: چون سشن ایزوله‌ست، هدایت کاربر بین `/s/{slug}` قدیمی و
ساب‌دامین جدید ممکنه سشنش رو قطع کنه — جزئیات در قدم ۵ پایین.

### قدم ۵ — سمت کد Laravel — ✅ پیاده‌سازی شد (۲۰۲۶-۰۹-۱۹، جزئیات در `Rasta_unified_prompt.md`)
- تشخیص تننت از ساب‌دامین با همون middleware موجود (`ResolveSalonFromRoute`) انجام می‌شه —
  بدون تکرار منطق، چون `Route::domain('{salon_slug}.'.central_domain)` پارامتر `salon_slug` رو
  دقیقاً مثل یک بخش URI پر می‌کنه.
- مسیر `/s/{slug}` فعلی **حذف نشد** و همیشه ثبت می‌مونه، دقیقاً همون‌طور که این فایل از اول
  می‌گفت — به‌عنوان fallback واقعی، هم‌زمان با ساب‌دامین (نه به‌جاش).
- کل مکانیزم پشت یک کلید config جدید (`config('app.central_domain')`، از `env('CENTRAL_DOMAIN')`)
  فعال می‌شه: خالی = فقط `/s/{slug}` (رفتار قبلی، صفر ریسک برای تست‌های موجود)؛ مقداردهی‌شده =
  `/s/{slug}` **و** ساب‌دامین هر دو زنده. این یک تصمیم سطح-boot است (نه per-request)، عمداً،
  چون این پروژه در production از `php artisan route:cache` استفاده می‌کنه.
- دامنه‌ی اصلی بدون ساب‌دامین فعلاً یک صفحه‌ی placeholder ساده نشون می‌ده (نه لندینگ کامل) —
  تصمیم تأییدشده با ابوالفضل؛ لندینگ واقعی با محور «۴. ثبت‌نام عمومی سالن» میاد.
- ⚠️ **ریسک شناخته‌شده و پذیرفته‌شده (نه حل‌شده)**: چون `SESSION_DOMAIN` ایزوله است (نه مشترک —
  تصمیم دیگه‌ای که با ابوالفضل تأیید شد)، وقتی کاربری از لینک قدیمی `/s/{slug}` وارد بشه و بعد
  یک لینک داخلی (که حالا خودکار به ساب‌دامین اشاره می‌کنه) کلیک کنه، به یک هاست دیگه navigate
  می‌شه و کوکی سشنش اونجا موجود نیست — ممکنه logout به نظر برسه. این هنوز تست دستی واقعی نشده
  (فقط در سندباکس شبیه‌سازی و مستند شده)؛ اگه در عمل مزاحم شد، دو راه‌حل احتمالی: `SESSION_DOMAIN`
  مشترک (با ریسک امنیتی خودش که قبلاً رد شده) یا یک صفحه‌ی میانی «داری منتقل می‌شی» قبل از
  redirect نهایی.

---

## چک‌لیست سریع قبل از deploy واقعی
- [ ] دامنه ثبت شده و به سرور اشاره می‌کند (رکورد `A` برای `@`)
- [ ] رکورد wildcard (`*`) ساخته شده و resolve می‌شود (`nslookup test.domain.ir`)
- [ ] وب‌سرور (Apache/Nginx) با `ServerAlias`/`server_name` واید‌کارت، هر ساب‌دامینی را به همان
      `public/` پروژه می‌فرستد
- [ ] گواهی SSL wildcard صادر و نصب شده (یا Cloudflare Proxied فعال است)
- [ ] `cron` تمدید خودکار SSL (اگر از روش certbot dns-api استفاده شد)
- [ ] تصمیم `SESSION_DOMAIN` با ابوالفضل هماهنگ شده — ✅ حل شد: ایزوله (`null`)، نه مشترک
- [ ] تصمیم رفتار دامنه‌ی اصلی بدون ساب‌دامین مشخص شده — ✅ حل شد: صفحه‌ی placeholder ساده
- [ ] `CENTRAL_DOMAIN` در `.env` واقعی (production) روی دامنه‌ی واقعی ست شده (نه خالی)
- [ ] ریسک قطعی‌شدن سشن هنگام هدایت بین `/s/{slug}` و ساب‌دامین (به قدم ۵ نگاه کن) با کاربر
      واقعی روی محیط production تست شده، نه فقط سندباکس